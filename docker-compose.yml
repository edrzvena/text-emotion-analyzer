services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: nrc-emotion-lexicon
    container_name: nrc-emotion-lexicon
    ports:
      - "3000:80"
    restart: unless-stopped
